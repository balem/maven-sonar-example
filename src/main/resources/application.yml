server:
  port:8082
spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.MariaDBDialect
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/sonarexample?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: sonaruser
    password: sonaruser
  thymeleaf:
    cache: false